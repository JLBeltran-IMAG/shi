class messages:
    def __init__(self):
        self.open_detector = "Open detector"
        self.close_detector = "Close detector"

        self.start_transfer = "Start transfer"
        self.abort_transfer = "Abort transfer"
        self.stop_detector = "Stop transfer"
        self.free_transfer = "Free transfer"

        # # motor
        # self.start_detector = "Staring detector"
        # self.start_detector = "Staring detector"
        # self.start_detector = "Staring detector"
        # self.start_detector = "Staring detector"
        # self.start_detector = "Staring detector"

